name: Marketplace Action smoke

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  mock-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: ./
        with:
          config: examples/starter/mock-benchmark.json
          package-version: "2.8.0"

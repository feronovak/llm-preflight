# Product map

**Status:** scaffolded

What this tool does today. Present tense only — what is open belongs in the
roadmap, what shipped belongs in the changelog.

<!-- TODO(project-standard): every row below is a candidate read off module
     names and the CLI reference, not a verified claim. Confirm each against the
     code and mark it live, partial or dropped. Nothing here derives from the
     source automatically, and a plausible-looking claim nobody checked is worse
     than an empty table. -->

| Feature | State | Verified against |
|---|---|---|
| cross-provider preflight checks before a model switch | unverified | |
| CLI entry point `llm-preflight` | unverified | |
| MCP server entry point `llm-preflight-mcp` | unverified | |
| configuration file and schema | unverified | |
| benchmark mode | unverified | |
| result output contracts | unverified | |

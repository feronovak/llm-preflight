"""project-standard — validate and scaffold project documentation, release
flow and git hygiene.

Stdlib only, on purpose: this package is vendored into repos that must gain no
dependency, and runs on CI images with no setup step.
"""

VERSION = "0.1.0"
